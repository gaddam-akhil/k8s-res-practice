apiVersion: v1
kind: Namespace
metadata:
  name : nginx
  labels:
    project: roboshop
    component: frontend
    tier: frontend

# namespace is isolated space in k8 cluster were we can create our project related resource , in k8s cluster we can create mutliple , with
# scure , without of realtion of any projec 
# apiVersion: v1                  # Core Kubernetes API
# kind: Namespace                 # Resource type
# metadata:
#   name: nginx                   # Namespace name
#   labels:
#     project: roboshop            # Project identifier
#     component: frontend          # Component type
#     tier: frontend               # Application tier