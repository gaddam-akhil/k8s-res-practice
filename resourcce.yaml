apiVersion: v1
kind: Pod
metadata: 
  name: mongodb
  labels:
    project: roboshop 
    component: frontend 
    tier: frontend
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
     requests:
       memory: "60Mi"    
       cpu: "100m"
    limts:
      memory: "100Mi"
      cpu: "150m"   
